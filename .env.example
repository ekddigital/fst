# MySQL connection string for Prisma — set in .env.local (never commit real values)
# Format: mysql://DB_USER:DB_PASSWORD@DB_HOST:3306/fst
DATABASE_URL="mysql://DB_USER:DB_PASSWORD@DB_HOST:3306/fst"

# Public site URL for metadata and links
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
