# MySQL connection string — set in .env.local (never commit real values)
DATABASE_URL="mysql://user:password@localhost:3306/fst"

# Public site URL for metadata and links
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
